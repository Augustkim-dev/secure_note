# Phase 2: Notebook (노트북 기능)

## 목표

노트북 CRUD 및 화면 구현, 항목 표시/관리 기능 완성

---

## 태스크 목록

### 2.1 데이터 모델
- [ ] notebooks 테이블 스키마 구현
  ```sql
  CREATE TABLE notebooks (
    id TEXT PRIMARY KEY,
    title TEXT NOT NULL,
    icon TEXT DEFAULT '📝',
    content TEXT,
    order_index INTEGER DEFAULT 0,
    created_at TEXT,
    updated_at TEXT
  );
  ```
- [ ] Notebook 모델 클래스
- [ ] Item 모델 클래스 (content 내부)
- [ ] NotebookRepository 구현

### 2.2 노트북 목록 화면 (홈)
- [ ] 노트북 목록 UI
- [ ] 노트북 카드 컴포넌트
  - 아이콘, 제목
  - 항목 수, 최근 수정 시간
- [ ] 빈 상태 UI
- [ ] 설정 버튼 (우측 상단)

### 2.3 노트북 CRUD
- [ ] 노트북 생성 다이얼로그
  - 이름 입력
  - 이모지 아이콘 선택
- [ ] 노트북 수정 기능
- [ ] 노트북 삭제 (확인 다이얼로그)

### 2.4 기본 노트북 초기화
- [ ] 앱 최초 실행 시 기본 노트북 3종 생성
  - 🔑 아이디/비밀번호
  - 🏦 은행 계좌
  - 📞 전화번호
- [ ] 기본 노트북 삭제 방지 (선택적)

### 2.5 노트북 상세 화면
- [ ] 헤더 (뒤로가기, 제목, 편집 버튼)
- [ ] 항목 목록 (ListView)
- [ ] 추가 버튼 (하단)

### 2.6 항목(Item) 표시
- [ ] 항목 카드 컴포넌트
  - 서비스명 (📌)
  - 아이디
  - 비밀번호 (마스킹)
- [ ] 비밀번호 마스킹 (••••••••)
- [ ] 보기 토글 버튼 (👁)
- [ ] 클립보드 복사 버튼 (📋)
- [ ] 복사 시 토스트 메시지

### 2.7 항목 관리
- [ ] 항목 탭 → 편집 모달
- [ ] 항목 스와이프 삭제
- [ ] 삭제 확인 다이얼로그

---

## 기술 고려사항

### Content JSON 구조
```json
{
  "items": [
    {
      "id": "uuid-1",
      "service": "네이버",
      "username": "hong123",
      "password": "pass123!",
      "memo": "",
      "created_at": "2024-01-15T09:00:00Z"
    }
  ]
}
```

### Notebook 모델
```dart
class Notebook {
  final String id;
  final String title;
  final String icon;
  final List<Item> items;
  final int orderIndex;
  final DateTime createdAt;
  final DateTime updatedAt;

  // JSON 직렬화/역직렬화
}
```

### Item 모델
```dart
class Item {
  final String id;
  final String service;
  final String username;
  final String password;
  final String memo;
  final DateTime createdAt;
}
```

### 이모지 선택 UI
- 미리 정의된 이모지 목록 제공
- GridView로 선택 UI 구현

### 정렬
- 노트북: 최근 수정순 (updated_at DESC)
- 항목: 생성순 (created_at ASC) 또는 사용자 지정

---

## 의존성/선행 조건

- Phase 1 완료 (DB, 인증)

---

## 완료 기준 (Definition of Done)

- [ ] 노트북 생성/수정/삭제가 정상 동작한다
- [ ] 기본 노트북 3종이 자동 생성된다
- [ ] 노트북 목록이 최근 수정순으로 정렬된다
- [ ] 항목의 비밀번호가 마스킹 처리된다
- [ ] 보기 토글로 비밀번호를 확인할 수 있다
- [ ] 클립보드 복사가 동작한다
- [ ] 항목 편집/삭제가 정상 동작한다

---

## 예상 소요 시간

약 1주
